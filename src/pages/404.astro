---
import NewsLayout from "@/components/news/NewsLayout.astro";
import NewsNotFoundState from "@/components/news/NewsNotFoundState.astro";
import Layout from "@/layouts/main.astro";
import { getLocaleFromPath, t } from "@/lib/i18n";
import { buildNewsIndexPath } from "@/lib/news-routes";

const locale = getLocaleFromPath(Astro.url.pathname);
const basePath = import.meta.env.BASE_URL;
---

<Layout lang={locale} noindex={true}>
  <NewsLayout
    locale={locale}
    eyebrow="404"
    title={t(locale, "newsNotFoundTitle")}
    intro={t(locale, "newsNotFoundBody")}
  >
    <NewsNotFoundState
      slot="default"
      indexHref={buildNewsIndexPath(locale, basePath)}
      locale={locale}
    />
  </NewsLayout>
</Layout>
